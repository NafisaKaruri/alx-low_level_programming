#include "main.h"

/**
 * mul - multiplies two integers
 * @a: one of the integers to be multiplied
 * @b: one of the integers to be multiplied
 *
 * Return: the product of a and b
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
