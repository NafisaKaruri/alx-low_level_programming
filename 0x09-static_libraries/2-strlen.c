#include "main.h"
#include <string.h>

/**
 * _strlen - return the length of a string
 * @s: the string that should get its length
 *
 * Return: length of s
 */
int _strlen(char *s)
{
	size_t length;

	length = strlen(s);
	return (length);
}
