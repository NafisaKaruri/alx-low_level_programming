#include "main.h"
#include <ctype.h>

/**
 * _islower - cheks for lowercase character
 * @c: character to be checked
 *
 * Return: on success 1, otherwise 0
 */

int _islower(int c)
{
	return (islower(c));
}
